spring:
  datasource:
    url: jdbc:postgresql://crossover.proxy.rlwy.net:22247/railway
    username: postgres
    password: DIXMGqEqZyZubTNSOlqradqHuvBPYxhY
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  resources:
    static-locations: classpath:/static/

server:
  port: 8080
